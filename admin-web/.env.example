# Frontend request base path used by axios.
# Local development can keep this as /api and let Vite proxy requests.
VITE_API_BASE_URL=/api

# Backend target used by the Vite dev server proxy.
# Point this to the Spring Boot backend when running npm run dev.
VITE_API_PROXY_TARGET=http://localhost:8080
